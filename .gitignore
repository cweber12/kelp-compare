__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/
*.duckdb
*.duckdb.wal

# Data zones are local; only the registry is versioned
data/*
!data/registry/
!data/**/.gitkeep

# Machine- and session-specific Claude Code permissions; the portable,
# team-useful ones live in the committed .claude/settings.json
.claude/settings.local.json

.DS_Store
.env
